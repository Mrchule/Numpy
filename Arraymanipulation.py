import numpy as np

arr=np.array([[1,2,3,4,5,6,7],[4,3,5,63,23,23,54],[4,3,5,63,23,23,54]])

print("original array :",arr)

print("shape of the array is ",np.shape(arr)) # this function return a key value pair of the given array (no of array , number of element in that array)

print("length of the array :",len(arr)) # this len function return the no of array in that mdimentioal array